version: '3.8'
services:
  booking-app:
    build: .
    container_name: \
    restart: unless-stopped
    ports:
      - "\:8080"
    environment:
      - DATABASE_URL=file:/app/data/database.sqlite
    volumes:
      - \:/app/data
